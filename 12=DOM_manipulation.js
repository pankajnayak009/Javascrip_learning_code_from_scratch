// What is .outerHTML?
//.outerHTML is a DOM property that lets you get or replace
// the entire HTML element including itself (not just its inner content like .innerHTML).
// Example ->
// let heading = document.querySelector('h1');
// heading.outerHTML='<h2 style="color:red;">Replaced Heading</h2>';

//What is setAttribute()?
//The setAttribute() method sets a new value to an HTML attribute
// (not CSS properties directly, but things like style, class, id, etc.)
// element.setAttribute("attributeName", "value");
// exapmlw-> styles.setAttribute('class','divclass');
